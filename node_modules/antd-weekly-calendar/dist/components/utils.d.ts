import { WeekObject, EventsObject, GetWeekDates, GenericEvent } from './types';
export declare const daysToWeekObject: <T extends GenericEvent>(events: T[], startWeek: Date) => WeekObject<T>;
export declare const getDayHoursEvents: <T extends GenericEvent>(value: GetWeekDates, weekObject: WeekObject<T> | undefined) => EventsObject<T>[];
export declare const MIN_BOX_SIZE = 40;
export declare const sizeEventBox: <T extends GenericEvent>(event: T, hour: Date) => {
    boxPosition: number;
    boxSize: number;
};
